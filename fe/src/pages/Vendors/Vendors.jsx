import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

export default function Vendors() {
  return (
    <Card>
      <Title level={2}>Vendors</Title>
      <p>Vendors management page - Under development</p>
    </Card>
  );
}
